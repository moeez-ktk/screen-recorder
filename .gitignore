# Build output: build.ps1 recreates it (the app and the listener), and
# legacy/tools/build-listener.ps1 the old Electron-era listener
build/
legacy/tools/*.exe

# The Electron build's dependencies: npm install restores them
node_modules/
